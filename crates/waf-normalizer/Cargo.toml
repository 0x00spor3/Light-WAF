[package]
name = "waf-normalizer"
version = "0.1.0"
edition = "2021"

[dependencies]
waf-core = { path = "../waf-core" }
unicode-normalization = "0.1"
serde_json = "1"
tracing = "0.1"

[dev-dependencies]
bytes = "1"
